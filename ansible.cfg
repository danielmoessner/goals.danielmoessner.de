[defaults]
# Running from the repo root uses this inventory by default.
inventory = ansible/inventory/production/hosts.ini
interpreter_python = auto_silent
retry_files_enabled = False

stdout_callback = default
